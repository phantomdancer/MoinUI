{
  "name": "moin-ui",
  "version": "0.1.0",
  "description": "MoinUI 墨影UI - macOS SwiftUI 组件库",
  "scripts": {
    "gen-version": "bash scripts/gen-version.sh",
    "predev": "pnpm gen-version",
    "dev": "swift build && nodemon",
    "build": "swift build",
    "test": "swift test --enable-code-coverage 2>&1 | grep -v -E '(CoreData|XPC|started|passed|^Test Case|^Test Suite.*passed|^\\s*Executed.*with 0 failure)' | grep -v '^$' || true && pnpm test:coverage",
    "test:coverage": "bash scripts/coverage.sh",
    "test:coverage:html": "xcrun llvm-cov show .build/debug/MoinUIPackageTests.xctest/Contents/MacOS/MoinUIPackageTests --instr-profile=.build/debug/codecov/default.profdata --sources Sources/MoinUI --format=html > coverage.html && open coverage.html",
    "clean": "swift package clean",
    "resolve": "swift package resolve",
    "update": "swift package update",
    "release": "pnpm gen-version && git add Sources/MoinUI/Utils/Version.swift && echo '版本文件已生成，请提交并创建tag推送'"
  },
  "keywords": [
    "swift",
    "swiftui",
    "macos",
    "ui"
  ],
  "author": "phantomdancer",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/phantomdancer/moin-ui.git"
  },
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}