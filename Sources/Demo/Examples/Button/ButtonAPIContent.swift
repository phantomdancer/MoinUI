import SwiftUI
import MoinUI

struct ButtonAPIContent: View {
    var body: some View {
        ScrollView {
            ButtonAPISection()
                .padding(Moin.Constants.Spacing.xl)
        }
    }
}
