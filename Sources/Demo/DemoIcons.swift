import Foundation

/// 全局图标定义
struct DemoIcons {
    // Overview
    static let introduction = "house"
    static let quickStart = "play.circle"
    
    // Development
    static let theme = "paintbrush"
    static let token = "square.stack.3d.up"
    static let configProvider = "gearshape"
    static let localization = "globe"
    static let colors = "paintpalette"
    
    // General
    static let button = "rectangle.and.hand.point.up.left"
    static let typography = "textformat.size"
    static let tag = "tag"
    
    // Data Display
    static let badge = "app.badge"
    static let avatar = "person.crop.circle"
    static let empty = "tray"
    
    // Layout
    static let space = "rectangle.split.3x1"
    static let divider = "minus"
    
    // Other (Playground specific)
    static let seed = "leaf"
    static let global = "globe" // Note: localization also uses globe in NavItem, might be confusing but fine.
}
