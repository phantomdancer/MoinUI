import SwiftUI

// MARK: - Moin.SpaceSize

public extension Moin {
    /// Space size
    enum SpaceSize {
        case small
        case middle
        case large
        case custom(CGFloat)
    }
}
